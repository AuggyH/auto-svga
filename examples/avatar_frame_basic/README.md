# Avatar Frame Basic Example

This example contains one transparent PNG avatar frame and an `asset.config.json` file.

Run:

```bash
pnpm build:example
```

Output:

```text
examples/avatar_frame_basic/output/
```
