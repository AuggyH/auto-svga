# P6 Repair 6 Worker Context Packet: A3 Electron Host And Visible Normal App

Worker ID: A3
Role: Electron Host And Visible Normal App
Branch: `agent/codex/p6-r6-a3-visible-normal-app`
Visible thread id: `019eeb7e-072c-7382-afe5-330eb92b9d2f`
Current integration base commit: `85c1ae0dea9ea8e6349a6616eed3b1e9714cdc61`

## Product Objective

P6 validates a bounded desktop host path while preserving the browser workflow
as rollback.

## Current Repair Finding

Repair 6 external review found Desktop host paths and normal App proof invalid:
proof-mode env/query was used, `windowShown=false`, and secondary/reference/latest
desktop paths lacked actual loaded/playback proof. A3 may not broaden Electron
scope or claim production desktop readiness.

Rejected Repair 4 evidence for terminal proof:

- old normal App proof
- old desktop screenshots
- old packaged App proof

## Repair 6 Task

Complete host-owned Desktop parity for:

- secondary SVGA file select and drag-drop
- reference media select and drag-drop
- latest artifact scan/load in Export Review
- File > Open and local file lifecycle
- normal visible App startup via `open -n "<Auto SVGA.app>"` or equivalent
  Finder-equivalent launch, without proof/smoke flags, proof query, or proof
  environment variables
- local-only bridge, logs, settings, process exit, and temp cleanup evidence

Use allowlisted roots and main-process scanning only. Do not expose arbitrary
filesystem access to the renderer and do not leak absolute paths into reports
or logs.

## Frozen Contract Paths

- `docs/loop/CURRENT_MILESTONE.md`
- `docs/product/P6_HOST_ADAPTER_CONTRACT.md`
- `docs/product/P6_SHARED_FRONTEND_ARCHITECTURE.md`
- `docs/product/p6/P6_MULTI_AGENT_COORDINATION.md`
- `docs/product/p6/P6_WORKER_REGISTRY.json`

## Owned Files

- Electron main process files
- Electron preload files
- `ElectronHostAdapter`
- host-only IPC tests
- Desktop latest-artifact adapter
- file, menu, and drop host integration

## Prohibited Files

- `docs/loop/CURRENT_MILESTONE.md`
- `docs/loop/LOOP_STATE.md`
- `docs/loop/LOOP_HISTORY.jsonl`
- `AGENTS.md`
- root `package.json`
- final handoff inputs
- `tools/p6/generate-p6-evidence.mjs`
- broad Web frontend parent paths
- macOS packaging output

## Dependencies

- A2 shared frontend integration must be available for host integration.

## Acceptance Criteria Responsibility

- Implement: P6-AC-07, P6-AC-11, P6-AC-12, P6-AC-13
- Evidence comes from A3 targeted tests, A5 package checks, A4 parity helpers,
  and A0 serial runtime proof.

## Required Tests

- Host adapter contract tests
- IPC validation tests
- Electron source smoke when assigned by A0
- No arbitrary filesystem or remote navigation regressions
- Latest-artifact tests proving non-empty real results when fixtures exist and
  safe failure when no allowlisted artifact exists
- Normal App path checks without proof/smoke query/env, proving
  `windowShown=true`, `normalVisibleStartup=true`, `rendererQuery=""`,
  `environmentOverrides={}`, no orphan processes, and no external requests

## Required Handoff Format

Commit all Worker changes, keep workspace clean, and report:

- `baseCommit`
- `headCommit`
- `commits`
- `changedFiles`
- `tests`
- `assumptions`
- `blockers`
- `requestedIntegrationChanges`

Do not merge. Do not modify terminal loop state. Do not generate final P6
Review Packet.

## Rejected Patterns

- Relaxing CSP or sandbox as a production baseline
- Foreground App popups during automated debugging
- Arbitrary directory static serving
- Recording local absolute paths in tracked files
- Claiming production desktop readiness
