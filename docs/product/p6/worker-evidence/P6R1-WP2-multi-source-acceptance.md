# P6-R1 WP2 Multi-source Acceptance Evidence

Date: 2026-06-25
Worker: P6R1-WP2-MultiSource-Acceptance
Base commit: de0ddae95d2068543f79ae2bfe224ebc475a92f0

## Failure-First Baseline

Headless Chrome against the local Web Preview first proved the current baseline
had no executable WP2 vertical acceptance entry:

- page: `http://127.0.0.1:4198/tools/svga-player-preview/`
- product shell loaded: true
- state probe installed after build: true
- `runWp1StateCorrectnessFlow`: function
- `runWp2MultiSourceAcceptanceFlow`: undefined

Before build, the same runtime path also showed the shared product app could not
load because `dist/` was missing. After preparing `dist/` with `npm run build`,
the failure narrowed to the missing WP2 multi-source acceptance flow.

## Repair Summary

- Added `runWp2MultiSourceAcceptanceFlow` to the shared product runtime probe.
- The flow drives real product paths: primary SVGA file drop, second SVGA file
  drop, synthetic reference GIF file load, `/api/latest-artifact` scan/load, and
  a manual SVGA source switch after latest artifact load.
- Added cleanup assertions for secondary SVGA state after leaving compare mode,
  reference media after manual source load, and latest artifact state after
  manual source load.
- Cleared stale reference/latest state on manual SVGA load.
- Cleared the secondary SVGA slot when switching into export review mode.
- Added source-sharing guard strings for the WP2 runtime probe and cleanup
  checks.

## Passing Runtime Evidence

Headless Chrome against the local Web Preview passed:

- flow: `Primary SVGA -> Second SVGA -> Reference Media -> Latest Artifact -> Manual Cleanup`
- `usedRuntimeLoadPath`: true
- `directStateInjection`: false
- `primarySvgaLoaded`: true
- `secondSvgaLoaded`: true
- `secondSvgaStatePassed`: true
- `secondSvgaSyncActive`: true
- `referenceMediaLoaded`: true
- `referenceSyncActive`: true
- `latestArtifactLoaded`: true
- `latestArtifactRuntimeLoaded`: true
- `secondaryClearedAfterExport`: true
- `referenceClearedAfterManualSource`: true
- `latestArtifactClearedAfterManualSource`: true
- failures: []

## Validation

- `node --check tools/shared/product-frontend/product-app.mjs` passed.
- `node --test tools/shared/product-frontend/source-sharing.test.mjs` passed.
- `npm run build` passed.
- `npm run build:example` passed.
- `npm run export:example` passed and produced the local ignored SVGA fixture.
- Headless Chrome WP2 runtime multi-source acceptance flow passed on
  `http://127.0.0.1:4198/tools/svga-player-preview/`.
- `node --test tools/svga-player-preview/inspection-report-view.test.mjs tools/svga-player-preview/server-inspection-report.test.mjs` passed.
- `npm --prefix tools/electron-prototype/experiments/svga-web run spike:svga-web:prepare` passed.
- `npm --prefix tools/electron-prototype/experiments/svga-web run spike:svga-web:test` passed.

## Notes

- Dependency preparation used the bundled pnpm executable because `pnpm` and
  `corepack` were not on PATH.
- The bundled pnpm install returned `ERR_PNPM_IGNORED_BUILDS` after placing
  dependencies, but `npm run build` and all targeted tests passed afterward.
- The temporary `pnpm-workspace.yaml` generated by dependency preparation was
  removed and is not part of the worker changes.
- Generated verification outputs under `dist/`, `node_modules/`,
  `examples/avatar_frame_basic/output/`, and Electron `.runtime/` directories
  are ignored and are not part of the worker commit.
