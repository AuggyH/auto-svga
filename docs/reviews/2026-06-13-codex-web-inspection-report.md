# Review: Web avatar-frame inspection report

## 1. Summary

Exposed the existing avatar-frame production specification report in the Web
preview Overview panel through a minimal local HTTP host boundary.

## 2. Git state

- Branch: `agent/codex/web-inspection-report`
- Commit before work: `712a5fe`
- Implementation commit: `a74fd18`
- Uncommitted changes before work: none
- Untracked assets staged: none

## 3. Changed files

- `tools/svga-player-preview/server.mjs`
- `tools/svga-player-preview/main.js`
- `tools/svga-player-preview/inspection-report-view.mjs`
- `tools/svga-player-preview/styles.css`
- `tools/svga-player-preview/inspection-report-view.test.mjs`
- `tools/svga-player-preview/server-inspection-report.test.mjs`
- `tools/svga-player-preview/README.md`

## 4. Requirement checks

| Requirement | Status |
|---|---|
| Read-only inspection report in existing Web preview | Done |
| Pass/fail, spec ID, summary, issues, calibration notes | Done |
| Issue severity, code, and message | Done |
| File-size/resource-count product calibration notice | Done |
| Reuse existing report service and checker chain | Done |
| Node/filesystem logic kept outside UI | Done |
| Preserve playback, import, drag-drop, and compare | Verified |
| No new dependency or format | Done |
| Narrow-width overflow and vertical-text checks | Verified |

## 5. Verification

Validation tier: Tier 2.

```text
npm run build
PASS

node --test tools/svga-player-preview/inspection-report-view.test.mjs tools/svga-player-preview/server-inspection-report.test.mjs
5 passed

node --test dist/tests/avatar-frame-inspection-report.test.js dist/tests/avatar-frame-production-spec.test.js dist/tests/svga-motion-spec-checker.test.js dist/tests/motion-inspection-service.test.js
18 passed

node --test --test-name-pattern="Web job loader" dist/tests/mvp-planner.test.js
1 passed

node --check tools/svga-player-preview/main.js
node --check tools/svga-player-preview/server.mjs
PASS
```

Browser smoke:

- Real job loaded and produced the expected failed report for 30 FPS.
- Local Preview and Compare still rendered correctly.
- 1280, 760, 480, and 360 px widths had no body, panel, or report horizontal overflow.
- Report text remained `horizontal-tb`.
- No browser console errors.

Full regression was skipped because exporter, playback implementation, build
configuration, dependencies, and cross-cutting runtime contracts were not
modified.

## 6. Regression and drift

- SVGA exporter: not touched; generated bytes unchanged.
- CLI default flow: not touched.
- Web player implementation: not touched.
- Import and drag-drop handlers: not touched.
- Comparison controls: not touched.
- Scope remains avatar-frame SVGA specification display only.

## 7. Dependencies and client readiness

- Dependencies added: none.
- License impact: none.
- Browser view is host-neutral and only consumes JSON.
- Node/protobuf/filesystem concerns stay behind the local preview server.
- The report view can be reused by future macOS and Windows shells.
- Desktop packaging must include compiled `dist/workbench` modules and
  `proto/svga.proto`, as already required by the current Node inspector.

## 8. Risks

- The information panel reports SVGA A, matching the existing panel ownership;
  SVGA B has no independent report selector.
- File-size and resource-count thresholds remain placeholders pending product calibration.
- The local inspection endpoint limits request bodies to 25 MB.

## 9. Next steps

- Calibrate avatar-frame file-size and resource-count production thresholds.
