# Owner Fix Report: ASV-QA-20260708-002

## Summary

- Owner: Short-term Main Engineer
- Lane: Short-term Preview
- Ticket: `/Users/huangtengxin/.codex/worktrees/ed2a/auto-svga/docs/quality/tickets/ASV-QA-20260708-002.md`
- Fix status: Fix ready
- Fix commit: See final `Fix Ready` handoff for the committed hash.
- Branch: `agent/codex/short-term-preview-qa-20260708`

## Owner Reproduction

- Reproduced by owner: Yes
- Environment: macOS desktop client, exact owner build identity pending QA.
- Notes: Owner reported a `512 x 512` replacement appearing oversized in a `96 x 96` `avatar` slot. Production assets and screenshots are not committed.

## Root Cause

- The short-term replacement workflow passed the selected PNG bytes through to the output preview.
- When the selected replacement image had larger pixel dimensions than the original imageKey resource, the embedded resource dimensions changed and the player displayed it at the larger size.

## Change Summary

- Changed files:
  - `src/workbench/short-term-image-replacement-workflow.ts`
  - `src/tests/short-term-image-replacement-workflow.test.ts`
  - `src/tests/short-term-image-replacement-preview-session.test.ts`
- Behavior changed: Short-term Preview replacement now reads the original embedded imageKey dimensions and normalizes mismatched replacement PNGs to those dimensions before creating preview or Save As bytes.
- Behavior not changed: The generic `SvgaImageResourceEditor` still preserves its original contract and replaces bytes exactly as requested by lower-level callers.
- Product scope check: Matches PRD S12 and the Replaceable Element Definition: short-term replacement images should match original resource dimensions unless a clear fit/crop rule is shown. The implemented short-term policy is `resize_to_original_resource_dimensions`.

## Validation

- Commands:
  - `npm run build`
  - `node --test dist/tests/short-term-product-model.test.js dist/tests/short-term-image-replacement-workflow.test.js dist/tests/short-term-image-replacement-preview-session.test.js dist/tests/svga-image-resource-editor.test.js dist/tests/nq1-r1-hardening.test.js`
  - `npm run test:all`
  - `npm run desktop:smoke`
- Results: All listed commands passed in this implementation thread.
- Manual checks: Not performed against the exact owner production SVGA and replacement image because those assets are local-only and not committed.
- Not run: Owner exact `96 x 96` slot plus owner exact `512 x 512` replacement visual confirmation.
- Reason for skipped checks: QA still needs the exact owner material for independent regression.

## Risk

- Regression risk: Medium. Replacement normalization changes short-term Preview behavior for mismatched replacement PNG dimensions.
- Protected flows: Reset clears the active replacement and returns to source bytes; generic editor tests still pass, proving the short-term policy did not leak into lower-level editor behavior.
- Known limitations: The current fit policy uses deterministic nearest-neighbor resizing to preserve slot dimensions. It does not expose a user-selectable contain/crop mode in the frozen short-term scope.

## QA Callback Criteria

- Original steps QA must rerun:
  - Open a file with a designer-named imageKey whose original embedded resource is `96 x 96`.
  - Replace it with a `512 x 512` PNG.
  - Confirm Preview remains bounded to the original slot/resource dimensions.
  - Reset the replacement and confirm there is no stale oversized state.
- Adjacent checks QA should run:
  - Replace with a same-size PNG and confirm it passes through without unnecessary normalization.
  - Save As after replacement and reopen to confirm the embedded image remains the original slot size.
- Evidence QA should expect:
  - Replacement summary records source dimensions separately from final embedded replacement dimensions.
  - `normalizedToOriginalDimensions=true` and `fitPolicy=resize_to_original_resource_dimensions` for mismatched replacements.

## Handoff

- Returned to QA at: See final `Fix Ready` handoff.
- Thread/message note: Implementation owner returns `Fix Ready`; QA owns regression and closure.
