# Owner Fix Report: ASV-QA-20260708-003

## Summary

- Owner: Short-term Main Engineer
- Lane: Short-term Preview / Replaceable Elements
- Ticket: `/Users/huangtengxin/.codex/worktrees/ed2a/auto-svga/docs/quality/tickets/ASV-QA-20260708-003.md`
- Fix status: Fix ready
- Fix commit: See final `Fix Ready` handoff for the committed hash.
- Branch: `agent/codex/short-term-preview-qa-20260708`

## Owner Reproduction

- Reproduced by owner: Yes
- Environment: macOS desktop client, exact owner build identity pending QA.
- Notes: PM clarified in `c2a6a850` that designer-named raster/imageKey placeholders such as `text1`, `text2`, `from`, and `to` are short-term S13 runtime text preview targets.

## Root Cause

- Text-semantic key detection was too narrow. It handled some text-like names but did not reliably classify simple deterministic names with numeric suffixes or short labels such as `from` and `to`.
- As a result, those keys could remain in the ordinary imageKey replacement group and expose image-only actions.

## Change Summary

- Changed files:
  - `src/workbench/short-term-product-model.ts`
  - `src/tests/short-term-product-model.test.ts`
- Behavior changed: The product model now classifies deterministic text-semantic imageKeys including `text1`, `text2`, `from`, `to`, `sender`, `receiver`, `content`, `copy`, `label`, `desc`, and `message` into the runtime text group.
- Behavior not changed: Ambiguous non-automatic imageKeys remain image replacement targets. No OCR, visual recognition, external AI, or artwork-content guessing is used.
- Product scope check: Matches PRD S7/S13 and the 2026-07-08 PM clarification.

## Validation

- Commands:
  - `npm run build`
  - `node --test dist/tests/short-term-product-model.test.js dist/tests/short-term-image-replacement-workflow.test.js dist/tests/short-term-image-replacement-preview-session.test.js dist/tests/svga-image-resource-editor.test.js dist/tests/nq1-r1-hardening.test.js`
  - `npm run test:all`
  - `npm run desktop:smoke`
- Results: All listed commands passed in this implementation thread.
- Manual checks: Not performed against the exact owner production SVGA because that asset is local-only and not committed.
- Not run: QA visual confirmation that the reported file no longer shows image-only actions on `text1` / `text2`.
- Reason for skipped checks: QA still needs the exact owner file and build identity for independent regression.

## Risk

- Regression risk: Low to medium. The classifier changes grouping for deterministic text-like names, which is the intended PM clarification.
- Protected flows: Automatic image names such as `img_000` remain excluded from replaceable elements; ordinary non-text designer imageKeys remain in the image replacement group.
- Known limitations: If the player bridge cannot apply dynamic text at runtime for a detected text target, the UI must show the existing explicit unsupported fallback instead of image replacement actions.

## QA Callback Criteria

- Original steps QA must rerun:
  - Open the reported file or equivalent with `text1` / `text2`.
  - Confirm those rows appear in the runtime text/text element group.
  - Confirm they no longer show `替换预览图片` or `重置预览图片`.
  - Confirm supported rows show inline text input/reset, or a truthful unsupported text-preview fallback.
  - Confirm text preview does not create dirty bytes or write back to SVGA.
- Adjacent checks QA should run:
  - Confirm `avatar` or another image-like designer key still appears in the image replacement group.
  - Confirm key rename still follows S11/S14 dirty/save behavior.
- Evidence QA should expect:
  - Product model classification includes text examples and excludes the same keys from image replacement rows.

## Handoff

- Returned to QA at: See final `Fix Ready` handoff.
- Thread/message note: Implementation owner returns `Fix Ready`; QA owns regression and closure.
