# Owner Fix Report: ASV-QA-20260709-002

## Summary

- Owner: Short-term Main Engineer
- Lane: Short-term macOS client / Preview right information surface
- Ticket: `docs/quality/tickets/ASV-QA-20260709-002.md`
- Related requirement: `docs/product/requirements/ASV-REQ-20260709-002.md`
- Product version: `Auto SVGA 0.1.x / SVGA Preview MVP`
- Release stage / channel: `Auto SVGA 0.1.0-alpha local`
- Fix status: Fix Ready
- Fix commit: `fa0ca9f6ed5beb589b00dd1593d8379abb87476d`
- Branch: `agent/codex/short-term-preview-qa-20260708`

## Root Cause

- The runtime structure model already marked secondary diagnostics with `disclosure: "moreInfo"`, but the overview render model only returned default visible facts.
- Non-risk secondary facts were filtered out instead of being grouped into an expanded right-panel disclosure.
- The installed owner-visible package therefore contained friendly runtime structure labels, but no `更多信息` renderer or secondary-details state.

## Change Summary

- Added `overviewFactGroups(model)` to split overview facts into default `summary` facts and expandable `moreInfo` facts.
- Preserved risk promotion: `warning` and `fail` facts marked as `moreInfo` still appear in the default summary.
- Added `createOverviewMoreInfoDisclosure` with `details/summary`, `RuntimeStructureMoreInfo`, `factMoreInfo`, and visible `更多信息` copy.
- Added scoped component styling using existing design tokens.
- Added `short-term-runtime-structure-more-info.test.mjs` to assert low-risk secondary diagnostics remain accessible and package-inspection markers exist.

## Validation

- `node --test tools/electron-prototype/experiments/svga-web/tests/short-term-runtime-structure-more-info.test.mjs`: PASS, 2/2.
- `git diff --check` on touched implementation/test files: PASS.
- `npm run build`: PASS.
- `node --test dist/tests/runtime-structure-diagnostics.test.js dist/tests/svga-image-optimizer.test.js dist/tests/short-term-optimization-workflow.test.js dist/tests/short-term-product-model.test.js dist/tests/asset-intelligence.test.js`: PASS, 19/19.
- `npm --prefix tools/electron-prototype/experiments/svga-web run spike:svga-web:test`: PASS, 39/39. The first sandboxed attempt hit `listen EPERM` on `127.0.0.1`; rerun with local listener permission passed.

## Skipped Checks / Limitations

- Owner-visible installed app regression was not run in this implementation lane because `/Users/huangtengxin/Applications/Auto SVGA.app` still needs a Release/Packaging or clean local-stable refresh from a descendant of `fa0ca9f6ed5beb589b00dd1593d8379abb87476d`.
- No production SVGA, screenshot, video, generated output, or owner local package extraction was committed.
- Foreground UI/manual real-material checks remain QA-owned after the owner-visible package contains the fix.

## QA Callback Criteria

- Verify the owner-visible package contains `overviewFactGroups`, `createOverviewMoreInfoDisclosure`, `RuntimeStructureMoreInfo`, `factMoreInfo`, and visible `更多信息`.
- Verify default summary still shows risk-causing runtime structure fields without requiring expansion.
- Verify low-risk secondary diagnostics are accessible after expanding `更多信息`.
- Verify default user-facing UI does not expose `SpriteEntity`, `FrameEntity`, `图层数`, or `图层过多` as runtime structure labels.

## Handoff

- Handoff status: `Fix Ready`.
- Callback evidence: `docs/quality/evidence/ASV-QA-20260709-002/callback-evidence.json`.
- Packaging need: refresh `Auto SVGA 0.1.0-alpha local` at `/Users/huangtengxin/Applications/Auto SVGA.app` to include `fa0ca9f6ed5beb589b00dd1593d8379abb87476d` or a descendant before QA owner-baseline regression.
