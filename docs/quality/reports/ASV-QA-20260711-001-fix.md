# Owner Fix Report: ASV-QA-20260711-001

## Summary

- Owner: 0.2 Multi-format Main Engineer
- Lane: 0.2 multi-format
- Ticket: ASV-QA-20260711-001
- Fix status: Fix Ready
- Fix commit: branch head at handoff
- Branch: `codex/0.2-alpha2-foreground-intake-fix`

## Owner Reproduction

- Reproduced by owner: Yes, by source/package identity inspection.
- Environment: `/Users/huangtengxin/.codex/worktrees/116b/auto-svga`, branch `codex/0.2-alpha2-foreground-intake-fix`, base `421b083b93f4deacb2cb18c8ad6f7a042990b7f3`.
- Notes: The installed `Auto SVGA 0.2.0-alpha.2` bundle had correct version/build stamps but no launch environment carrying `AUTO_SVGA_PRODUCT_MILESTONE`. The source main process defaulted to `short-term`, which explains why QA could open SVGA while Lottie/VAP attempts stayed on the prior SVGA surface without a visible 0.2 state.

## Root Cause

- The alpha2 package had runtime dependencies and version identity for 0.2, but its packaged runtime identity did not tell the Electron main process to enter `0.2-multiformat-preview`.
- `tools/electron-prototype/experiments/svga-web/main.cjs` selected `productMilestoneId` from `AUTO_SVGA_PRODUCT_MILESTONE` or defaulted to `short-term`.
- The internal package script wrote `.runtime/build-info.json` with only `buildCommit` and `source`, so the installed package could start as formal `short-term` and expose only the SVGA open/preload/menu path.

## Change Summary

- Changed files:
  - `tools/electron-prototype/experiments/svga-web/main.cjs`
  - `tools/electron-prototype/experiments/svga-web/scripts/package-internal-trial.mjs`
  - `tools/electron-prototype/experiments/svga-web/tests/svga-web-experiment.test.mjs`
  - `docs/product/requirements/ASV-REQ-20260709-003.md`
  - `docs/quality/reports/ASV-QA-20260711-001-fix.md`
  - `docs/reviews/2026-07-11-codex-asv-qa-20260711-001-alpha2-foreground-intake-fix.md`
  - `docs/retrospectives/TASK_RETRO_LEDGER.jsonl`
  - `docs/retrospectives/PROJECT_LESSONS_CANDIDATES.md`
- Behavior changed: Packaged internal alpha builds now stamp `.runtime/build-info.json` with `productMilestoneId: "0.2-multiformat-preview"`. The main process reads that package-only runtime identity when no launch environment override exists, then installs the 0.2 multi-format menu/preload/session path.
- Behavior not changed: Development/default source launches still default to `short-term`; formal `0.1.x` preload/menu/open paths remain SVGA-only; hidden multi-format IPC guards remain in place for non-0.2 callers.
- Product scope check: This repairs package startup mode binding only. It does not claim Lottie/VAP product support, visual playback success, save/export/conversion, packaging readiness, release readiness, or Product Owner acceptance.

## Validation

- Commands:
  - `node --test --test-name-pattern "0.2 alpha package runtime identity|0.2 multi-format desktop mode|formal 0.1 direct multi-format IPC" tools/electron-prototype/experiments/svga-web/tests/svga-web-experiment.test.mjs`
  - `node --check tools/electron-prototype/experiments/svga-web/main.cjs`
  - `node --check tools/electron-prototype/experiments/svga-web/scripts/package-internal-trial.mjs`
  - `node --test --test-name-pattern "macOS package proof manifest records audit boundaries|0.2 alpha package runtime identity|0.2 multi-format desktop mode|formal 0.1 direct multi-format IPC|formal preload isolates short-term|formal 0.2 multi-format preload" tools/electron-prototype/experiments/svga-web/tests/svga-web-experiment.test.mjs`
  - `npm run build`
  - `npm run test:all`
  - `git diff --check`
- Results:
  - Focused package/mode/intake regressions passed 3/3 and expanded focused set passed 6/6.
  - Syntax checks passed.
  - Build passed.
  - Full root regression passed 524/524.
  - Diff whitespace check passed.
- Manual checks:
  - Read installed Info.plist metadata; confirmed installed alpha2 had version/build identity but no `AUTO_SVGA_PRODUCT_MILESTONE` environment.
- Not run:
  - No foreground app run, no installed-app mutation, no package generation, and no owner-material open was performed.
  - The entire Electron experiment test file was not used as the acceptance gate.
- Reason for skipped checks:
  - Foreground/package work needs a separate PM/Packaging permit. The whole Electron experiment file includes unrelated environment-sensitive checks that failed locally for missing `@electron/asar` fixture support and one token-bound server path returning 404; targeted package-mode and 0.1/0.2 guard regressions passed.

## Risk

- Regression risk: Medium. The repaired path is Electron main-process product-mode selection for packaged builds.
- Protected flows:
  - Formal `0.1.x` remains `short-term` by default in development/non-packaged runs.
  - 0.2 package mode is selected only from an explicit environment override or a packaged runtime build-info marker with an allowed value.
  - Direct hidden multi-format IPC calls remain guarded before host side effects in formal 0.1.
- Known limitations:
  - The already-installed `/Users/huangtengxin/Applications/Auto SVGA.app` is not changed by this source repair.
  - QA must receive a rebuilt/reinstalled alpha2 repair candidate before rerunning the foreground Lottie/VAP matrix.

## QA Callback Criteria

- Original steps QA must rerun:
  - After Packaging rebuilds and installs the repaired candidate, rerun the installed-app foreground matrix for SVGA-A, LOTTIE-A, and VAP-A using the same identity-bound foreground protocol.
- Adjacent checks QA should run:
  - Confirm installed package build info selects `0.2-multiformat-preview` without relying on process environment.
  - Confirm formal 0.1 protections remain absent from the 0.2 foreground test's support claims: no save/export/conversion, no product support claim, no release readiness claim.
  - Confirm Lottie/VAP failures, if any fixture-specific issue remains, show a 0.2 typed error/recovery state instead of silently retaining stale SVGA metadata.
- Evidence QA should expect:
  - A new installed alpha2 repair candidate from Release/Packaging.
  - Visible 0.2 multi-format intake state for Lottie/VAP or typed, path-redacted 0.2 error state; not silent retention of prior SVGA metadata.

## Handoff

- Returned to QA at: 2026-07-11 after commit and packet creation.
- Thread/message note: Fix Ready callback must go to replacement QA thread from canonical registry.
- QA thread: canonical replacement QA receiver from `docs/engineering/ACTIVE_DEVELOPMENT_LANES.md`.
- Handoff status: Pending send at commit time.
- If not sent, reason: n/a.
